java topics
->_________________methods_______________ 
->advantage of methods
->characteristics of methods
->types of methods

* based on argument                 based on modifiers
->no argument methods               ->statics methods
->parameterised methods             ->finds methods
var argument methods                ->abstract methods
                                    ->synchronised methods
                                    natives methods
===================================
(A)No argument methods
->a no argument methods cannot take any input as a formal argument inside the parenthesis.it perform operations on predefine values.

(B)parameterised methods
->a parameterised methods can take as a n num. input.these inputare use inside the methods to perform operations.

(C) variable argument methods
-> var arg methods allow passing a variable num.of arguments as formal arg inside the parenthesis. it is useful when the num of input is not fixed

Q1.What is methods.?
ans. A method is block of code or set of instructions is used to perform some specific task and its execute when it called 
it helps in 
.code reusebility
.modulity
.reduced duplication

Q2. What is syntax of method.?
ans.returnType methodName(parameters) {
    // body
}

Q3. what is method declaration and method calls
Ans. void methodName()  // this is method declaration
    methodName();       // this method calls

Q4. what is returnType.?
Ans.returnType define whats values method will return.
ex. int add(){return 5;}

Q5.what is a void method?
Ans. A method' doent not return any values when it call is called void method.

Q6.What is parameter and argument?
Ans.the parameter is variable in the method definition and the argument is the value passing by the wehen method call
example->
void add(int a ,int b) // parameters
add(5,6)   arguments

Q7 What is method overloading ?
Ans. method overloding meaning have multiple methods with same name but difference parameter
example-> void add(int a, int b) {}
          void add(int a, int b, int c) {}

Q8.what IS THE static method?
Ans. the method that belong to class and also called without creating an Objects

Q9.what is non-static method?
Ans. a non-static method is belong to Object and object craetion to call.

Q10. Can be non static method inside the static method?
Ans. yes , but only by craetion an object.
eg.-> 
 class demo{
    void show(){
        public static void main (String[] args){
            Demo obj= new Demo();
            obj.show();
        }
    }
 }


Q11. can we call non static method in static method
Ans. yes we can call non static method in static method but creating with an object then call non static method in static method
eg-> class Demo{
    clas  show(){
        public static void main(String[] arg){
            Demo obj =new Demo();
            obj.show();
        }
    }
}

Q12. What happens if we don't call a method?
Ans. its run but does not executes

Q. what is var args method ?
Ans . 
var args allow method passing variable numbers of arguments.

eg->void sum(int... nums){};














->methods over loading 
->statics bindding
->_________________________________________
Classes
Objects
constructor
Inheritance
Polymorphism
Encapsulation
Abstraction
Instance
Message Passing